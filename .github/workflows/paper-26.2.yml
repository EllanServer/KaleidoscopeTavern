name: Paper 26.2 build

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  staging:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Temporary staging workflow; restored by the queued patch commit."
