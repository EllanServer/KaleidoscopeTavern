pack:
  install-on-startup: true
  overwrite-managed-files: true

custom-crops:
  install-managed-config: true

stations:
  press-eject-invalid: true

# 自定义酒效 HUD 的渲染方式。
#   mode:
#     auto:     检测到 CustomNameplates 时停用内置 BossBar，改由外部通过
#               %kaleidoscopetavern_effect_hud% 占位符渲染；否则使用内置 BossBar。
#     builtin:  始终使用内置 BossBar。
#     external: 始终停用内置 BossBar，仅提供 PlaceholderAPI 占位符。
#   style:
#     corner: 原版药水 HUD 位置——右上角 24x24 效果框，增益第一行、
#             其余第二行，从右向左每 25px 一格，无文字（与原版一致）。
#     line:   顶部居中一行：图标 + 本地化名称 + 等级 + 倒计时。
#   gui-half-width: corner 风格的右缘基准 = 客户端 GUI 宽度的一半（像素）。
#     1920x1080 且 GUI 比例 4（自动）时为 240；GUI 比例 3 → 320；2 → 480。
#     其他分辨率的玩家 HUD 仍在顶部同一行，只是水平位置略有偏移。
effect-hud:
  mode: auto
  style: corner
  gui-half-width: 240

gameplay:
  # 与 Forge 版 GeneralConfig.InfiniteLavaFromTap 的默认值一致：默认无限。
  # 设为 false 后熔岩龙头变为有限，成功抽取后会消耗源炼药锅。
  infinite-lava-from-tap: true

# 藤架传播的四季限制（由 CustomCrops 的世界季节驱动）。
# 悬挂葡萄作为 CustomCrops 作物，其季节、温室和自然生长规则只配置在
# customcrops/contents/crops/kaleidoscope_tavern.yml，不在 Java 或本节重复。
# 以下默认值与 Forge 版“静谧四季（SereneSeasons）”方块标签一致：
#   grapevine-trellis      葡萄藤架的藤蔓攀爬生长：春、夏
#   gold-grapevine-trellis 金葡萄藤架：夏
#   ice-grapevine-trellis  冰葡萄藤架：冬
# 语义：
#   - 世界季节为 DISABLE（该世界未启用 CustomCrops 季节）时不做任何限制，
#     对应 Forge 版“未安装季节模组则不限制”；
#   - 季节不符时，若 CustomCrops 启用了温室（greenhouse），会按其
#     greenhouse 范围设置向上扫描温室玻璃，找到即放行（与 CustomCrops
#     自身 season requirement 的温室豁免完全一致）；
#   - 只拦截藤架的随机刻传播；骨粉传播不受季节限制（与 SereneSeasons 一致）；
#   - 野生葡萄藤（wild_grapevine）与 Forge 版一致，不受季节限制。
# 季节取值：spring / summer / autumn / winter（大小写不敏感）；
# 列表留空表示任何季节都不自然生长（骨粉与温室仍然有效）。
seasons:
  enable: true
  grapevine-trellis: [spring, summer]
  gold-grapevine-trellis: [summer]
  ice-grapevine-trellis: [winter]

bottle-placement:
  water: true
  honey: true
  potion: true
  dragon-breath: true
  experience: true

# 野生葡萄藤世界生成已迁移至 CraftEngine 的 configured/placed feature
# (内容包 configuration/worldgen.json),在真实区块生成管线内执行,
# 无需运行时开关;调整密度请编辑该文件后 /ce reload all。

messages:
  prefix: '<dark_aqua>[森罗酒馆]</dark_aqua> '
  no-recipe: '<red>当前材料无法完成配方。</red>'
  station-busy: '<yellow>这个设备正在工作。</yellow>'
  pack-missing: '<red>CraftEngine 内容包尚未加载，请执行 /ce reload all。</red>'
  press-filled: '<green>压榨完成：<amount>/1000 mB。</green>'
  barrel-started: '<green>酒桶已开始酿造。</green>'
  barrel-brewing-cannot-open: '<red>无法在酿造过程中打开酒桶！</red>'
  barrel-fluid-not-full: '<yellow>需要先把酒桶装满流体，才能添加原料！</yellow>'
  barrel-no-space: '<yellow>酒桶已满，无法添加更多原料！</yellow>'
  # 香炉开关提示已迁入生成的 CE furniture events(tools/migrate_legacy.py)。
